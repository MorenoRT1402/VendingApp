{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}
    <h1>{{ product.name }}</h1>

    <p>Precio: {{ product.price|format_currency('EUR') }}</p>

    <a href="{{ path('app_product') }}">Volver a la lista</a>
{% endblock %}